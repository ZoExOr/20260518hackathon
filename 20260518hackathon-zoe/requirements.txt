requests>=2.31
# optional, for offline tuning:
scikit-optimize>=0.9      # Bayesian optimisation (harness falls back to random)
# optional, for the LLM regime supervisor:
litellm>=1.40             # any provider via AGENT_MODEL
# dev:
pytest>=7.0
